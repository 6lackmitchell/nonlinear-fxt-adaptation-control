"""physical_params.py

Defines the physical parameters involved in simulating the Rossler
model in the standard situation.

Required Parameters:
    U_MAX

"""

import numpy as np

# Control input constraints
U_MAX = np.array([1.0])
